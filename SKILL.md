---
name: doc-format-check
description: 检查并规范化中文 Markdown 排版，并按词典修正中英文错别字。当用户要求处理/检查/校验文档格式、规范排版、统一专名或空格、修正错别字，或抱怨中英文没空格、全半角标点混用、列表标题错乱时使用。自动修正只覆盖「改法唯一」的机械项，需要判断的问题列成待确认清单，全部改动产出改前/改后对比记录。项目专名与业务词表用外挂 profile JSON 提供。不做内容改写、事实核对或文档格式转换。Normalize Chinese Markdown typography and fix typos, with a before/after record.
---

# 中文 Markdown 格式检查

## 1. 目的

**格式问题分两档，混在一起做必出事故。**

- **A 档**：改法唯一，不触碰语义——空格、标点全半角、列表标记、专名、必错词。自动修。
- **B 档**：需要事实或人的判断——有歧义的词、第三方名称归属、菜单名、有序列表是否真为顺序。**只报不改。**

判档唯一标准：**不看原始材料、不问相关方，能不能确定唯一正确写法。** 能 = A，不能 = B。

规则来源优先级：**项目 profile > [中文文案排版指北](https://github.com/sparanoid/chinese-copywriting-guidelines) > 个人习惯**。指北标注为「争议」的条目（链接前后加空格、直角引号）**不进 A 档**。

## 2. 前置

- Python 3，无第三方依赖。命令按平台取 `python3` 或 `py -3`。
- 中文乱码先设 `PYTHONIOENCODING=utf-8`。
- **目标必须是 Markdown**。`.md` 扩展名但内容是纯文本稿的不适用。
- 改之前必须有回滚点：Git 工作区干净，或先备份到扫描范围**之外**。

### 四种 harness

同一份 bundle，不做分叉。

| 环境 | 用户级 | 项目级 | 调用 |
| --- | --- | --- | --- |
| Claude Code | `~/.claude/skills/` | `.claude/skills/` | 按 description 自动匹配 |
| Codex | `~/.codex/skills/`、`~/.agents/skills/` | `.codex/skills/`、`.agents/skills/` | `/skills` 或 `$` |
| DeepSeek Harness | `~/.dsh/skills/`、`~/.agents/skills/` | `.dsh/skills/`、`.agents/skills/` | `/doc-format-check` |
| WorkBuddy | `WORKBUDDY_SKILLS_DIR` 或 `--dest` | 同左 | 技能选择器 |

```bash
python3 <skill-dir>/scripts/install.py --list          # 只探测
python3 <skill-dir>/scripts/install.py                 # 装用户级
python3 <skill-dir>/scripts/install.py --project .     # 装项目级
python3 <skill-dir>/scripts/install.py --dest <目录>
```

安装器先做契约自检（frontmatter、kebab-case name、目录名一致、无嵌套 SKILL.md），不通过即中止。装完重启 harness。`~/.agents/skills/` 是 Codex 与 DSH 共用目录。

`<skill-dir>` 是本文件所在目录，四个环境各不相同，不要写死。

## 3. 调用

### 3.0 profile

省略 `--profile` 时从**目标路径**逐级向上找 `.docformat.json`，找不到用 `generic`。

| 场景 | 参数 |
| --- | --- |
| 通用文档 | 省略 |
| 项目有专名/菜单/第三方名称 | 省略（自动发现）或 `--profile <路径>` |
| 强制忽略项目配置 | `--profile generic` |

新建 profile：复制 `profiles/example.json` → 改名 `.docformat.json` → 放项目根目录。不要提交进技能目录。

### 3.1 定范围 + 建回滚点

只处理用户指定的文件或目录。无 Git 时：

```bash
mkdir -p "<项目>/_backup/<子目录>" && cp <目标> "<项目>/_backup/<子目录>/"
```

### 3.2 只扫不改

```bash
export PYTHONIOENCODING=utf-8
python3 <skill-dir>/scripts/check_format.py "<文件或目录>"
```

不写文件。先看 A 档里有没有超出格式范畴的东西，有就回 3.1 缩范围。`--tsv` 出制表符格式。

### 3.3 应用 A 档 + 对比记录

```bash
python3 <skill-dir>/scripts/check_format.py "<目标>" --fix --report "<范围外>/format-record.md"
```

`--fix` 只落 A 档。**涉及错别字修正时 `--report` 必须给**。记录文件放扫描范围之外。

**验收线：复跑一次，A 档必须归零。**

### 3.4 B 档进对话

编号清单进对话，不写成报告文件让用户自己翻。逐条确认后只改被确认的，其余一个字不动。

## 4. 判据

依据：**指** = 排版指北；**工** = Markdown 语法；**项** = profile。

### A 档 · 可自动修（16 条）

| ID | 规则 | 依据 | 说明 |
| --- | --- | --- | --- |
| F01 | 列表标记后补空格 | 工 | `-文字` / `1.文字` 不是合法列表项 |
| F02 | 中英文之间加空格 | 指 | 「在 LeanCloud 上」 |
| F03 | 项目专名 | 项 | profile `project_names` |
| F04 | 分节标题层级 | 项 | profile 设 `heading_level` 时才检查 |
| F05 | 标题/列表前后补空行 | 工 | 列表紧贴标题会渲染塌陷 |
| F06 | 清多余空白 | 工 | 行尾空格、全角空格、Tab、开头空行、连续多空格 |
| F07 | 路径分隔符统一 ` > ` | 项 | 仅路径标签行，或「」内含 `path_context` 词/已有 `>` |
| F08 | 路径标签去加粗 | 项 | `**前往路径：…**` → 普通文本 |
| F09 | 技术专名大小写 | 指 | `github`→GitHub、`ios`→iOS、`wifi`→Wi-Fi、`typec`→Type-C |
| F10 | 中文与数字间加空格 | 指 | 「花了 5000 元」。`--num-space never` 可关 |
| F11 | 数字与单位间加空格 | 指 | 「20 TB」「5000 mAh」 |
| F12 | 删全角标点旁空格 | 指 | `「iPhone ，」` →「iPhone，」 |
| F13 | 压缩重复标点 | 指 | `「！！！」` →「！」，只压同字符 |
| F14 | 半角标点转全角 | 指 | `,;:?!.()`，左侧须为汉字或中文右括号/右引号 |
| F15 | 全角数字/字母转半角 | 指 | `１０００` → 1000 |
| F16 | 错别字/错别单词 | 内置+项 | 中 23 / 英 81 条必错词，英文保留原大小写 |

**F16 准入门槛**：只有「该写法在任何语境下都不可能正确」的词对才能自动改。
`帐号`/`按装`/`seperate` 进 F16；`登陆`（登陆作战）、`须要`、`其它` 只能进 B08。
项目补充用 profile 的 `typo_fix`，关掉内置某条用 `typo_fix_disable`。
**词典驱动，不是全量拼写检查**，词典外的错字一律抓不到。

### 例外（删任何一条都会改坏文档）

- **保护区掩码**：代码、链接、图片、URL、域名、带扩展名文件名、版本号、时间，不参与检测与修改。
- **品牌白名单**：官方不带空格的写法（如 `豆瓣FM`）走 profile `no_space_brands`，F02 不加空格。
- **英文整句内半角标点保留**：`「Stay hungry, stay foolish.」`、`See (fig 1)?` 不转全角。
- **度数与百分比**：`90°`、`15%` 不加空格，且反向删掉已有空格。
- **`W` 不进单位表**：可能是功率也可能是「万」，交 B09。
- **行首语法前缀隔离**：`- `、`1. `、`> `、`### ` 后的空格是语法的一部分，先切下前缀再处理正文。
- **分隔线守卫**：`---` / `***` / `___` 不进 F01；YAML frontmatter 与围栏代码块整段跳过。
- **表格单元格内边距**：`|` 两侧的空格不算「标点旁空格」，F12 不动。

### B 档 · 只报不改（13 条）

| ID | 规则 | 依据 | 不能自动改的原因 |
| --- | --- | --- | --- |
| B01 | 内部标记残留 | 项 | 删了等于把未决问题掩埋 |
| B02 | 第三方名称待裁定 | 项 | 归属确认前替换就是编造功能 |
| B03 | 普通标签被加粗 | 项 | 有的加粗是界面原文强调 |
| B04 | 有序列表疑为并列 | 工 | 整块 ≥2 条为「标签：说明」式才报，整块报一次 |
| B05 | 常见问题缺「？」 | 项 | 可能内容本就不是问句 |
| B06 | 空小节 | 工 | 补内容还是删标题由内容决定 |
| B07 | 绝对化/不可核验表述 | 项 | 对外承诺必须相关方确认 |
| B08 | 疑似错字、重复字、易混词 | 项 | 有歧义的词要看语境 |
| B09 | `数字+W` 疑似当「万」 | 项 | 可能是功率、型号 |
| B10 | 路径写法不一致 | 项 | 哪个是真实界面脚本不知道 |
| B11 | H1 缺失或重复 | 工 | 标题文本要人定 |
| B12 | 不地道缩写 | 指 | 全称写法看语境 |
| B13 | 引号风格混用 | 指·争议 | 方向由项目定 |

**脚本抓不到的，人必须补看**：语义错字（多字、缺字、串词）、同一内容两处重复展开、菜单名与实际界面不符。
**规则噪音也是 bug**：一条 B 档规则若对正常文档大面积命中，要么加收敛条件，要么按块汇总。

## 5. 否决点（碰到就停）

- 没有回滚点就 `--fix`。
- 纯文本稿当 Markdown 扫。
- 用户只说「处理格式」，却动了措辞、结构、事实描述。
- 自作主张改 B 档，尤其 B02 第三方名称、B07 承诺性表述。
- 把指北的「争议」条目做成自动修。
- 把有歧义的词对放进 `typo_fix`。
- 改了错别字却不出对比记录。
- 把项目私有词表写进脚本或技能目录。
- 为某个 harness 单独改 bundle 结构。
- 范围外的文件被写入。
- 只给「详见报告文件」不给原句。

## 6. 输出

**第一段 · 已改（A 档）**，一行一类：

| # | 位置 | 改动 |
| --- | --- | --- |
| 1 | 第 25 行 | `-AI工具：` → `- AI 工具：` |

**错别字单列词级对比**：

| # | 位置 | 改前 → 改后 |
| --- | --- | --- |
| 1 | 第 3 行 | `帐号` → 账号；`按装` → 安装 |

**第二段 · 待确认（B 档）**：编号清单，每条「位置｜原文｜建议或具体问题」，一条一个问题。

**记录**：给出 `--report` 文件路径。

收尾：改了几个文件、复跑是否归零、还有哪些原句悬着。

## 7. 失败处理

| 症状 | 动作 |
| --- | --- |
| 控制台中文乱码 | 设 `PYTHONIOENCODING=utf-8`，不影响文件内容 |
| profile 没按预期生效 | 看输出头部回显；自动发现从**目标路径**向上找，跨目录批量时显式传 `--profile` |
| 找不到 profile | 脚本会打印查找过的两个绝对路径 |
| harness 看不到技能 | `install.py --list` 确认目录；DSH 要求目录名与 `name` 一致；装完重启 |
| A 档复跑不归零 | 规则不幂等。贴出仍在报的 ID，改**规则顺序**（加空格类必须早于 F12），不要手动抹平 |
| 改完列表或标题失效 | 查行首语法前缀是否被隔离 |
| 改出了不该改的 | 从备份还原整个文件，缩小范围重跑，不要打补丁 |
| 要加新规则 | 先判档，再定位置（加空格类早于 F12），跑五组测试确认零误伤且幂等 |
| 规则是项目专有的 | 写进 profile，不动脚本 |
