# 2.0 当前实现说明

间距使用固定UTR59-r1分类和内置uniseg0.10.0/Unicode16.0。当前行为以README、references/rules.md和references/spacing.md为准；下文是早期设计记录，旧“无第三方依赖”或自动词形修复说法以2.0规则覆盖。

> 当前行为以 SKILL.md、references/rules.md 和测试为准；下文保留设计依据，不构成各宿主兼容认证。

# doc-format-check 设计与核查文档

> 核查用。每条规则给出依据 / 触发条件 / 判档理由 / 可推翻点，盯最后一列。

---

## 一、7 字段骨架

每段有严格准入，写错地方会在运行时被误用：

| 字段 | 放什么 | **不放**什么 |
| --- | --- | --- |
| 1 目的 | 核心主张（一句能被反驳的话） | 功能罗列 |
| 2 前置 | 环境依赖、安装、不适用的输入、必须先建的回滚点 | 原理解释 |
| 3 调用 | 可直接复制的命令与顺序 | 原理解释 |
| 4 判据 | 规则表 + 例外 + 冲突裁定 | 操作步骤 |
| 5 否决点 | **碰到就停**的情形 | 一般性提醒 |
| 6 输出 | 固定格式，让产出跨会话一致 | 示例正文 |
| 7 失败处理 | 具体症状 → 具体动作 | 「请检查配置」式废话 |

---

## 二、三层结构（决定了它可以公开分享）

```
通用层（技能内置，无项目信息）
  · 指北规则：空格、标点、全半角、技术专名大小写
  · Markdown 语法正确性：列表标记、空行、分隔线、frontmatter
  · 错别字词典（常见中英文错字；字形/地区偏好默认只报告）
  · 单位表、保护区、判档框架
        ▲
        │ profile JSON 挂载
        │
项目层（放在项目根目录 .docformat.json，不进技能）
  · project_names   专名唯一写法
  · rival_marks     第三方名称（只报不改）
  · path_context    菜单路径上下文词
  · typo_fix        项目「必错」词（自动改）
  · typo_pairs      业务易混词（只报）
  · heading_level / num_space / quote_style
        ▲
        │
文档层（被处理的 .md）
```

**分界线**：一条规则若离开某个具体项目就失效，它属于项目层。脚本里不留任何项目数据。

**profile 解析顺序**：`--profile` 优先 → 按每个文件从**目标路径**向上找 `.docformat.json` → `generic`。自动发现让四个 harness 共用同一条命令。

---

## 三、多环境适配：一个 bundle，四个入口

四者消费同一个 SKILL.md bundle 契约。适配 = 按最严交集构造一份包 + 安装器处理路径差异。

### 最严交集

| 约束 | 来自 | 不满足的后果 |
| --- | --- | --- |
| frontmatter 必有 `name` + `description` | 四者共有 | 不被识别 |
| `name` 必须 kebab-case | DSH（正则校验） | 直接拒绝加载 |
| `name` 必须与目录名一致 | DSH（按目录名注册） | 注册成两个名字 |
| 全包只能有一个 SKILL.md | DSH（不支持递归发现） | 嵌套的那个永远不会被发现 |
| `description` 写「什么时候用我」 | DSH/Codex 启动只注入 name+description | 写成功能罗列会导致该触发时不触发 |
| 脚本与资源放子目录 | 四者共有 | —— |

DSH 与 Codex 每次会话启动都注入全部技能的 name+description，所以 description 要短、要写触发条件。

### 安装器负责差异

`scripts/install.py` 做三件事：

1. **契约自检**（preflight）——上表能机器验的四条：frontmatter、kebab-case、目录名一致、无嵌套 SKILL.md。**不满足就中止**，而不是装一份会被拒的包。
2. **探测**已存在的技能目录；`--list` 只报告不动文件；路径未知的环境显式提示，不静默跳过。
3. **安装**：复制或符号链接（POSIX 优先链接，Windows 无权限时自动回退复制）。

`~/.agents/skills/` 是 Codex 与 DSH 共用目录。WorkBuddy 路径未公开，靠 `WORKBUDDY_SKILLS_DIR` 或 `--dest`。

### 脚本本身的可移植性

- 只用标准库，无第三方依赖。
- `<skill-dir>` 由 `__file__` 推出，不写死任何绝对路径。
- profile 自动向上发现，不依赖 harness 的工作目录约定。
- 不联网、不读写技能目录之外的文件（除非显式给 `--report` 路径）。
- 文件写入固定 UTF-8；控制台乱码靠 `PYTHONIOENCODING`，不影响文件内容。

---

## 四、对话层流程

```
用户说「处理格式」
      │
      ▼
┌─────────────────────────────────────────┐
│ 3.1 定范围 + 建回滚点                    │
│   · 只取用户指定的文件/目录              │
│   · 有 Git → 确认工作区干净              │
│   · 无 Git → 备份到「范围外」目录        │
│     （备份落在范围内会被再扫一遍）       │
└─────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────┐
│ 3.2 只扫不改（不带 --fix）               │
│   输出 A 档 n 处 / B 档 m 处 + 生效 profile│
│   ★ 人工卡口：A 档里有没有超出格式范畴的？│
│      有 → 退回 3.1 缩范围                │
└─────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────┐
│ 3.3 --fix 落 A 档 + --report 出对比记录   │
│   ★ 验收线：复跑一次，A 档必须归零        │
│     不归零 = 规则不幂等 = 脚本 bug       │
└─────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────┐
│ 3.4 B 档进对话，编号清单                 │
│   「位置｜原文｜建议/问题」三件套         │
│   逐条确认 → 只改被确认的那些            │
└─────────────────────────────────────────┘
      │
      ▼
   diff 验证 → 回报
```

**核查点**：「只扫不改」防越界，「复跑归零」防脚本出错。两道闸门缺一，技能就退化成批量正则替换。

---

## 五、脚本内部数据流

```
读文件
  │
  ▼
skip_flags()  ──►  逐行「跳过表」
  │                 ① YAML frontmatter（开头 --- 包裹段）
  │                 ② ``` 围栏代码块
  ▼
对每一行（未跳过的）：
  │
  ├─ 1. mask()   保护区 → 占位符
  │        代码、链接、图片、URL、域名、文件名、版本号、时间、品牌白名单
  │
  ├─ 2. 分隔线守卫  --- / *** / ___ 原样返回
  │
  ├─ 3. F01 列表标记补空格（必须最先，后面要靠前缀识别行首语法）
  │
  ├─ 4. 切下行首 Markdown 语法前缀
  │        ^(\s*(?:[-*+]|\d+\.|>|#{1,6})\s+)
  │
  ├─ 5. 对「正文部分」按固定顺序跑 A 档规则（见第六节）
  │
  ├─ 6. 前缀 + 正文拼回，unmask() 还原保护区
  │
  └─ 7. 在「原始行」上跑 B 档检测（只记录，不改）
  ▼
块级规则（跨行才能判）
  B11 H1 数量 → F04 标题层级 → F05 空行 → F06 开头空行
  → B06 空小节 / B05 Q？A → B04 有序列表块 → B13 引号混用 → B10 路径不一致
  ▼
--fix ? 写回文件 : 只打印        --report ? 写对比记录 : 不写
```

**核查点**：第 7 步刻意在原文上判。代价是 B 档清单的原文片段可能已被 A 档改过——核查时以文件当前内容为准。

---

## 六、A 档规则顺序（顺序是正确性的一部分）

顺序错 = **不幂等**：

```
F01 列表标记 ──┐
               ├─► 必须在「切前缀」之前，否则前缀识别不出来
切前缀 ────────┘
   │
   ▼
F08 去加粗 → F15 全角转半角 → F16 错别字 → F03 项目专名 → F09 技术专名 → F07 路径分隔符
   │            替换类，互不依赖，顺序可换
   ▼
F14 半角标点转全角  ──┐
F13 压缩重复标点      ├─► 必须在 F12 之前：先把标点变成全角，F12 才认得出来
   ▼                  │
F02 中↔英加空格       │
F10 中↔数字加空格     ├─► 「加空格」三兄弟，必须在 F12 之前
F11 数字↔单位加空格   │
   ▼                  │
F12 删全角标点旁空格 ◄┘  ★ 最后：收拾前面三兄弟在标点边上留下的空格
   ▼
F06 压缩多余空白 + rstrip
```

**核查点**：新增「加空格」规则必须插在 F12 之前，否则 A 档永远不归零。

---

## 七、判档决策树

**唯一标准：不看原始材料、不问相关方，能不能确定唯一正确写法。**

```
              发现一个格式问题
                     │
                     ▼
        ┌──────────────────────────┐
        │ 改法唯一吗？              │
        │（换个人来改会一样吗）     │
        └──────────────────────────┘
             │否                │是
             ▼                  ▼
          ┌─────┐      ┌──────────────────────┐
          │ B档 │      │ 需要事实/原始材料     │
          └─────┘      │ 才能确认吗？          │
                       └──────────────────────┘
                            │是         │否
                            ▼           ▼
                         ┌─────┐  ┌──────────────────┐
                         │ B档 │  │ 是「争议」条目吗？ │
                         └─────┘  │（规范自己标注的） │
                                  └──────────────────┘
                                     │是        │否
                                     ▼          ▼
                              ┌────────────┐  ┌─────┐
                              │ B档，只做  │  │ A档 │
                              │ 不一致检测 │  └─────┘
                              └────────────┘
```

三个分支各有实例：

- **改法不唯一** → B11（H1 标题文本该叫什么）、B04（这串编号到底是不是顺序）
- **需要事实** → B02（第三方名称对应自家哪个功能）、B07（承诺能不能兑现）、B09（66W 是功率还是「万」）
- **规范标注争议** → B13（直角引号 vs 弯引号，指北明确列在「争议」节）

### 错别字最容易判错

看起来「改法唯一」，其实取决于词本身：

| 词 | 判断 | 归档 |
| --- | --- | --- |
| `帐号`、`按装`、`seperate` | 字符串本身不成词，任何语境都错 | F16 自动改 |
| `登陆` | 「登陆作战」合法 | B08 只报 |
| `须要` | 本身是词，与「需要」用法不同 | B08 只报 |
| `其它` | 风格偏好，不是错误 | B08 只报 |

F16 准入门槛是举证式的：**能举出一个合法用例，就不能自动改。**

### 自动改词必须留痕

F16 是唯一「不看上下文就替换实词」的规则，四条约束：

1. **`--report` 产出对比记录**，含词级 `改前 → 改后` 与所在整句。
2. **记录文件放在扫描范围之外**，否则下一轮会被当作正文处理。
3. **词典可关**：`typo_fix_disable` 按错误写法关掉内置任意条目。
4. 控制台输出对 A 档用「改前/改后」，对 B 档用「原文/建议」——两者语义不同，不能共用措辞。

对比记录的四段结构：按规则统计 → 错别字逐条词级对比 → 全部 A 档整行对比 → B 档待确认清单。

---

## 八、规则逐条核查表

依据：**指** = 中文文案排版指北；**工** = Markdown 语法正确性；**项** = profile 提供。

### A 档 · 可自动修（16 条）

| ID | 规则 | 依据 | 触发条件 | 判档理由 | **可推翻点** |
| --- | --- | --- | --- | --- | --- |
| F01 | 列表标记后补空格 | 工 | 行首 `-*+` 后紧跟非空白、且不是重复标记字符 | `-文字` 在 CommonMark 里不是列表，渲染器会整段吞掉 | 排除靠 `(?![-*+\s])`。若文档用 `-->` 之类行首符号，确认没被误伤 |
| F02 | 中英文之间加空格 | 指 | 汉字↔拉丁字母相邻 | 规范明文，无歧义 | 官方不带空格的品牌要进 `no_space_brands` |
| F03 | 项目专名 | 项 | profile `project_names` 命中 | 项目规范钦定唯一写法 | 词表由 profile 维护，脚本不内置 |
| F04 | 分节标题层级 | 项 | profile 设了 `heading_level` | 体例固定时无歧义 | **默认不检查**。只有 profile 显式设置才生效 |
| F05 | 标题/列表前后补空行 | 工 | 标题紧贴正文、列表块紧贴非列表 | 渲染塌陷头号原因 | 无 |
| F06 | 清多余空白 | 工 | 行尾空格、全角空格、Tab、开头空行、连续多空格 | 不可见字符，无语义 | 用「行尾两空格」做换行的写法会被清掉 |
| F07 | 路径分隔符统一 ` > ` | 项 | 路径标签行，或「」内含 `path_context` 词/已有 `>` | 项目规范钦定 | **只在这两个语境生效**，避免误伤正文连字符 |
| F08 | 路径标签去加粗 | 项 | `**<路径标签>…**` | 体例要求普通文本 | 标签词来自 profile |
| F09 | 技术专名大小写 | 指 | 词表内单词大小写不符 | 规范明文（`github`→GitHub） | **词表是人选的**（36 个）。`eSIM`/`Type-C` 靠精确映射，不能改成通用 upper |
| F10 | 中文与数字间加空格 | 指 | 汉字↔阿拉伯数字相邻 | 规范明文「花了 5000 元」 | **翻转点**：日期会变成「2026 年 9 月 18 日」。嫌密用 `--num-space never` |
| F11 | 数字与单位间加空格 | 指 | 数字后紧跟单位表内单词 | 规范明文「20 TB」 | **`W` 故意不在表内**，交 B09。单位表 20 个，可增 |
| F12 | 删全角标点旁空格 | 指 | 全角标点两侧的空格 | 规范明文 | **翻转点**：`A —— B` 会变 `A——B`。要保留就把 `—` 移出 FULL_PUNCT |
| F13 | 压缩重复标点 | 指 | 同一标点连续重复 | 规范明文 | 只压同字符重复，`！？` 组合合法 |
| F14 | 半角标点转全角 | 指 | 左侧是汉字或中文右括号/右引号，右侧是汉字或句末 | 规范明文 | **翻转点**：中文后的 `(NMRI)` 转全角括号。若惯用半角括号需改此条 |
| F15 | 全角数字字母转半角 | 指 | `１０００`、`ＡＢＣ` | 规范明文 | 指北的例外（设计稿对齐用全角）不适用于正文 |
| F16 | 错别字/错别单词 | 内置+项 | 词典命中（中 23 / 英 81 条） | **准入门槛**：该写法在任何语境下都不可能正确 | **门槛是唯一防线**。往词典里塞一个有歧义的词，就会在全库范围内改错 |

### B 档 · 只报不改（13 条）

| ID | 规则 | 依据 | 触发条件 | 判档理由 | **可推翻点** |
| --- | --- | --- | --- | --- | --- |
| B01 | 内部标记残留 | 项 | profile `internal_marks` | 删了等于把未决问题掩埋 | 词表由 profile 维护 |
| B02 | 第三方名称待裁定 | 项 | profile `rival_marks` | 归属确认前替换就是编造功能 | 同上 |
| B03 | 普通标签被加粗 | 项 | 行首 `**xx**：` | 有的加粗是界面原文强调 | 触发面可能偏宽 |
| B04 | 有序列表疑为并列 | 工 | 整块中 **≥2 条**是「标签：说明」式 | 是不是真有先后，要看内容 | **阈值 2 是拍的**。调高会漏，调低会吵 |
| B05 | 常见问题缺「？」 | 项 | `faq_heading` 小节下条目无问号 | 可能内容本就没写成问句 | 小节名来自 profile |
| B06 | 空小节 | 工 | 标题下无正文 | 补内容还是删标题由内容决定 | 无 |
| B07 | 绝对化表述 | 项 | profile `absolute_claims` | 对外承诺必须相关方确认 | 词表偏小 |
| B08 | 疑似错字 | 项 | profile `typo_pairs` + 内置重复字 | 有歧义的词对判定要看语境 | 无歧义的已归 F16。**这里只该留「举得出合法用例」的词** |
| B09 | W 疑似当「万」 | 项 | 数字+W | 可能是功率、型号 | 无 |
| B10 | 路径写法不一致 | 项 | 同一末级菜单出现多种完整路径 | 哪个是真实界面脚本不知道 | 只比对路径标签行与含 `>` 的「」 |
| B11 | H1 缺失/重复 | 工 | H1 数量 ≠ 1 | 标题文本要人定，文件名只是线索 | 代填等于替用户命名章节 |
| B12 | 不地道缩写 | 指 | h5/Ts/FED/RJS | 全称写法看语境 | 词表极小，中文文档几乎不命中 |
| B13 | 引号风格混用 | 指·争议 | 「」与“”并存 | **指北自己标为争议项** | 方向由项目定。不自动改是刻意的 |

---

## 九、八条例外（删任何一条都会改坏文档）

| 例外 | 保护什么 | 不做会怎样 |
| --- | --- | --- |
| 保护区掩码 | 代码/链接/URL/域名/文件名/版本号/时间 | 域名被专名规则改写；配置文件名被大小写规则改坏；`1.2.3` 被塞句号 |
| 品牌白名单 | 官方不带空格的写法 | F02 强行加空格，改出错别名 |
| 英文整句内半角标点保留 | `「Stay hungry, stay foolish.」`、`See (fig 1)?` | 英文句子里出现中文标点 |
| 度数/百分比不加空格 | `90°`、`15%` | 变成 `90 °`、`15 %`，指北明确列为错误 |
| `W` 不进单位表 | `66W` 功率 vs「万」 | 自动加空格掩盖了「用 W 表万」的问题 |
| 行首语法前缀隔离 | `- `、`1. `、`> `、`### ` 后的空格 | F12 把它当「全角标点旁空格」删掉，整行不再是列表 |
| 分隔线守卫 + frontmatter 跳过 | `---` / `***` / `___`、YAML 头 | F01 把 `---` 拆成 `- --`，frontmatter 报废 |
| 表格单元格内边距 | `\|` 两侧的空格 | F12 把整张表挤成一团 |

---

## 十、验证方法

改动脚本后五组都必须过：

| 测试集 | 内容 | 判据 |
| --- | --- | --- |
| ① 指北正反例 | 覆盖指北全部正确/错误示例 | 全过，**且负例不被误改**：英文句内标点、英文书名里的半角冒号、`See (fig 1)?`、品牌白名单、`66W` |
| ② Markdown 语法边界 | frontmatter、分隔线、行首加粗、表格、HTML 块、脚注 | frontmatter 与分隔线零改动；表格单元格与脚注内容正常规范化 |
| ③ 错别字词典 | 中英文错词、大小写变体、保护区内的同名错词、有歧义词 | 中英文均改对；`SEPERATE`→`SEPARATE` 保留大小写；代码与 URL 内的同一错词不动；有歧义词留在 B 档 |
| ④ 真实语料回归 | 一批真实文档 | A 档命中全部属实，零误伤 |
| ⑤ 幂等 | ①②③④ 各跑两遍 `--fix` | 第二遍 diff 全空，A 档归零 |

改动 bundle 结构时另加一组：`install.py --list` 的契约自检必须通过。

幂等是硬指标：A 档不归零说明规则在互相打架，此时「改完了」的结论不可信。

---

## 十一、已知缺口（明确不保证）

1. **语义错字抓不到**。F16 是**词典驱动**，不是全量拼写检查：词典外的错字、多字漏字、串词一律发现不了。**扫完 ≠ 文档没问题**，人必须通读。
2. **讲格式规则的文档**里，反例必须写进反引号，否则会被当成真错误改掉。本技能自身文档已按此写法处理，可自举。
3. **HTML 块内文本会被处理**。对正文是想要的，对手写 HTML 片段可能不是。
4. **纯文本稿（`.md` 扩展名但不是 Markdown）不适用**，会被加上不该有的结构标记。
5. **B 档清单的原文片段取自修复前**。核查时以文件当前内容为准。
6. **WorkBuddy 的技能目录路径未经官方文档确认**，靠环境变量或 `--dest` 指定；其是否额外要求 `skill.yml` 尚未验证。

---

## 十二、扩展流程

**加一条通用规则**（改脚本）：

1. 判档（第七节）。拿不准放 B 档：B 档误报只是噪音，A 档误改是事故。
2. A 档规则定位置：加空格类必须在 F12 之前（第六节依赖图）。
3. 写例外。
4. 跑五组测试（第十节），特别是幂等与真实语料零误伤。
5. 同步三处：脚本 `RULES`、SKILL.md §4、本文档第八节。

**加一条项目规则**（改 profile，不碰脚本）：复制 `profiles/example.json`，改名 `.docformat.json` 放项目根目录。

**改 bundle 结构**（新增文件、改目录名）：先看第三节的最严交集表，再跑 `install.py --list` 过契约自检。

---

## 十三、可配置项一览

| 配置 | 位置 | 默认 | 什么时候改 |
| --- | --- | --- | --- |
| `heading_level` | profile | `null`（不检查） | 体例固定分节标题层级时 |
| `num_space` | profile / `--num-space` | `always` | 嫌日期「2026 年 9 月」太密时设 `never` |
| `quote_style` | profile | `detect` | 不关心引号统一时设 `none` |
| `no_space_brands` | profile | `[]` | 出现官方不带空格的品牌名时 |
| `project_names` | profile | `[]` | 有专名唯一写法要求时 |
| `path_context` / `path_line_labels` | profile | 见 generic.json | 文档有菜单路径时 |
| `rival_marks` | profile | `[]` | 文档可能残留第三方名称时 |
| `typo_fix` / `typo_fix_disable` | profile | `[]` | 追加项目「必错」词，或关掉内置某条 |
| `typo_pairs` / `internal_marks` / `absolute_claims` | profile | 通用小词表 | 有业务专属易混词时 |
| `FULL_PUNCT` 里的 `—` | 脚本 | 含 | 要保留破折号两侧空格时移出 |
| `UNITS` | 脚本 | 20 个 | 出现新单位时增（**别加 `W`**） |
| `TECH_NAMES` | 脚本 | 36 个 | 出现新技术专名时增 |
| `BUILTIN_ZH_TYPOS` / `BUILTIN_EN_TYPOS` | 脚本 | 中 23 / 英 81 | 通用「必错」词，加之前先过准入门槛 |
| `WORKBUDDY_SKILLS_DIR` 等 | 环境变量 | 未设 | harness 技能目录不在默认位置时 |
